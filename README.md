# StillMotion Studio

静止画に、小さな動き・読み上げ・環境音・BGMを重ねて「疑似映像」を作るローカル編集ツールです。フルアニメーションを生成し直すのではなく、元絵の見た目を守りながら必要な部分だけを動かすことを目標にしています。

`main` は **v0.1基準版**、`work/v0.2-partial-motion-prep` ブランチは **v0.2プレビュー版** です。プレビュー版では、画像上を四角または自由な輪郭で選び、その範囲だけに小さな動きを付けられます。

## v0.1でできること

- 複数の静止画をシーンとして並べる
- 文章、表示秒数、カメラのズーム・パンをシーンごとに設定する
- 横長画像は「全体を表示」に切り替え、左右を切らずにぼかし背景つきで配置する
- 雨、風、葉、木漏れ日、蝶、光粒、雪、ほこりを重ねる
- 文字入り画像を固定する「文字保護モード」
- ブラウザTTSで読み上げを試聴する
- ナレーション、環境音、全体BGMを読み込む
- ナレーションと環境音を別々に音量調整し、環境音の長さをシーンごとに決める
- プロジェクトをJSONで保存・読込する
- Chrome / Edgeで音声入りWebMを書き出す

## 起動方法

Windowsでは `start.bat` をダブルクリックし、ブラウザで `http://localhost:4173` を開きます。

手動で起動する場合は、このフォルダで次を実行してください。

```bash
python -m http.server 4173
```

Pythonランチャーを使うWindows環境では、次でも起動できます。

```bat
py -m http.server 4173
```

依存ライブラリのインストールは不要です。

## サンプル作品

`sample/maru/` に、18ページの『まると おともだち』を収録しています。

- `01.webp`〜`18.webp`: ページ画像
- `project.json`: 作品を単独で読み込めるプロジェクトデータ
- `README.md`: 作品構成と素材追加時の置き場所
- `audio/README.md`: 後から追加する音声・BGMの命名方針

既存の画像パスを壊さないため、v0.1の `sample/maru/NN.webp` は移動していません。

## リポジトリ構成

```text
.
├── index.html                 # v0.1の画面
├── app.js                     # v0.1の編集・再生・書き出し処理
├── styles.css                 # v0.1の見た目
├── start.bat                  # Windows用ローカル起動
├── sample/maru/               # 『まると おともだち』
├── src/v0.2/                  # v0.2部分モーションの独立した下準備
├── tests/                     # Node標準機能だけで動く準備コードのテスト
└── docs/
    ├── DESIGN.md              # 全体設計と責務
    ├── PARTIAL_MOTION.md      # 部分モーションの具体設計
    └── ROADMAP.md             # バージョン別の作業順
```

## v0.2プレビューの使い方

1. 横長画像の端が切れる場合は、右側の「画像の表示」を「全体を表示」にします。
2. 右側の「部分モーション」で「＋ 四角で選ぶ」または「＋ 自由に囲む」を押します。
3. 中央のプレビュー画像で、動かしたい場所をドラッグして囲みます。自由選択では輪郭を一周なぞります。
4. 「動き」「動きの強さ」「動きの速さ」を選びます。
5. 「▶ 動きを見る」で確認します。
6. プロジェクトJSONへ保存すると表示方法と選択範囲も保存されます。

現在できること:

- `motionRegions` の保存形式
- 画像上の矩形選択と青い編集枠
- 指やマウスで輪郭をなぞる自由選択
- 揺れ、移動、波、炎、呼吸に対応するモーション種別
- 選んだ範囲だけのCanvasプレビューとWebM出力
- シーンごとの「画面いっぱい／全体を表示」切り替え
- 入力値の正規化と検証
- 時刻からTransform値を求める純粋関数
- 旧v0.1プロジェクトを読み込むための後方互換方針
- Node標準テストによるデータモデル・座標変換確認

準備コードの確認にはNode.js 20以降で次を実行します。

```bash
npm test
```

詳細は [部分モーション設計](docs/PARTIAL_MOTION.md) と [ロードマップ](docs/ROADMAP.md) を参照してください。

## 現在の制約

- ブラウザTTSは試聴用です。動画へ確実に入れる場合はナレーション音声ファイルを指定してください。
- ローカルで追加した画像・音声の本体はプロジェクトJSONへ埋め込みません。
- WebM書き出しは実時間です。2分の作品なら約2分かかります。
- 自由選択した輪郭の点は保存できますが、後から輪郭の一部だけを消すブラシは次の段階です。
- 背景の穴埋めは未実装です。動きを強くすると、元の絵との重なりが目立つ場合があります。

## 開発方針

v0.1は動作する基準として維持します。v0.2の計算処理は `src/v0.2/` に分離し、既存UIへ段階的に接続します。矩形選択で操作感と描画結果を確認してから、ブラシ選択と背景の穴埋めへ進みます。
